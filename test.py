
from __future__ import annotations
"""show cases for new graph system"""
import unittest, pprint
from matplotlib import pyplot as plt
import networkx as nx


if __name__ == '__main__':
	import chimaera
	# import importlib, importlib.util, sys, ast
	# import inspect
	#
	# targetPath = r"F:\all_projects_desktop\common\edCode\chimaera\core\datatype.py"
	#
	# spec = importlib.util.spec_from_file_location("chimaera.core.datatype", targetPath)
	# print(spec)
	# mod = importlib.util.module_from_spec(spec)
	# #mod = spec.loader.create_module(spec)
	# #print("module", mod)
	# #__import__(mod.__name__)
	# #print(mod.__name__ in sys.modules)
	#
	# # modText = open(targetPath, "r").read()
	# # mod = exec(modText)
	#
	#
	# mod = spec.loader.exec_module(mod)
	# print("loaded", mod)
	# print(mod.DataType, mod.Int)

	#
	#
	# print("modules in path results", results)
	#
	#
	#
	# # if not includeParent:
	# # 	results = results[1:]
	# if loadModules:
	# 	newResults = []
	# 	for i in results:
	# 		#print("i", i)
	# 		mod = importlib.util.module_from_spec(i)
	# 		#print("mod from spec", mod)
	# 		# try:
	# 		mod = i.loader.exec_module(mod)
	#
